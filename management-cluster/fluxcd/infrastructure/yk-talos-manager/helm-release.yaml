apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: yk-talos-manager
  namespace: yk-talos-manager
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: yk-talos-manager
    namespace: yk-talos-manager
  install:
    createNamespace: false
