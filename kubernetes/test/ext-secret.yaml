apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: vault-test
  namespace: external-secrets
spec:
  secretStoreRef:
    name: default-store
    kind: ClusterSecretStore
  target:
    name: example
  data:
    - secretKey: prd-secret
      remoteRef:
        key: prd
        property: k

