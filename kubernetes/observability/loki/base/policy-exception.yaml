apiVersion: policies.kyverno.io/v1beta1
kind: PolicyException
metadata:
  name: loki
  namespace: loki
spec:
  matchConditions:
    - name: exclude-loki
      expression: "object.metadata.namespace == 'loki'"
  policyRefs:
    - kind: ValidatingPolicy
      name: restrict-seccomp-strict
