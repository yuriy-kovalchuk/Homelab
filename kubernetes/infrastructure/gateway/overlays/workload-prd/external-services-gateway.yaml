apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: external-services
  namespace: external-gateway
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-dns
spec:
  gatewayClassName: cilium
  addresses:
    - type: IPAddress
      value: "10.0.4.60"
  listeners:
    - name: https-truenas
      port: 443
      protocol: HTTPS
      hostname: "truenas.yuriy-lab.cloud"
      tls:
        mode: Terminate
        certificateRefs:
          - name: truenas-gw-tls
            kind: Secret
      allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchLabels:
              kubernetes.io/metadata.name: "truenas"
