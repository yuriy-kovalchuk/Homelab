apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: yk-dns-manager
  namespace: argo-cd
spec:
  project: infrastructure
  source:
    repoURL: git@github.com:yuriy-kovalchuk/Homelab.git
    targetRevision: HEAD
    path: kubernetes/apps/yk-dns-manager/manifest
    helm:
      releaseName: yk-dns-manager
  destination:
    server: https://kubernetes.default.svc
    namespace: yk-dns-manager
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    managedNamespaceMetadata:
      labels:
        goldilocks.fairwinds.com/enabled: "true"
