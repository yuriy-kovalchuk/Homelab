apiVersion: v2
name: minio-yk
description: Umbrella Helm chart for minio application in Kubernetes

type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: minio
    version: "5.4.0"
    repository: "https://charts.min.io/"
