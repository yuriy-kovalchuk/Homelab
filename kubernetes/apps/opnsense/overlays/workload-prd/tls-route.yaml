apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TLSRoute
metadata:
  name: opnsense
  namespace: opnsense
spec:
  parentRefs:
    - name: external-services
      namespace: external-gateway
      sectionName: tls-opnsense
  hostnames:
    - "opnsense.yuriykovalchuk.dev"
  rules:
    - backendRefs:
        - name: opnsense
          port: 443
