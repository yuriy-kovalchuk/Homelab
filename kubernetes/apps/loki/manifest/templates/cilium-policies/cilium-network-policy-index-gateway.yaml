apiVersion: "cilium.io/v2"
kind: CiliumNetworkPolicy
metadata:
  name: "allow-index-gateway-to-access-minio"
  namespace: {{ .Release.Namespace }}
spec:
  description: "Allow Index Gateway to access Minio"
  endpointSelector:
    matchLabels:
      app.kubernetes.io/component: index-gateway
      app.kubernetes.io/instance: loki
  egress:
    - toEndpoints:
        - matchLabels:
            app: minio
            io.kubernetes.pod.namespace: minio
      authentication:
        mode: "required"