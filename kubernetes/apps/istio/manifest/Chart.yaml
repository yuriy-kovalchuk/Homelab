apiVersion: v2
name: istio-yk
description: Umbrella Helm chart for istio application in Kubernetes

type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: base
    version: "1.27.1"
    repository: https://istio-release.storage.googleapis.com/charts

