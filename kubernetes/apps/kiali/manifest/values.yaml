kiali:
  deployment:
    resources:
      requests:
        cpu: "10m"
        memory: "64Mi"
      limits:
        cpu: "100m"
        memory: "256Mi"

  external_services:
    prometheus:
      url: "http://grafana-prometheus-kube-pr-prometheus.monitoring:9090"