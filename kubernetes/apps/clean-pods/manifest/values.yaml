schedule: "0 * * * *"   # every hour
cleanup:
  failed: true
  succeeded: true
  succeededMaxAgeSeconds: 3600   # 1 hours
image:
  repository: "rancher/kubectl:v1.32.9-amd64"
  tag: latest
  pullPolicy: IfNotPresent
rbac:
  serviceAccountName: cleanup-sa
