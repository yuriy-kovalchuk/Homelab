schedule: "0 * * * *"   # every hour
cleanup:
  failed: true
  succeeded: true
  succeededMaxAgeSeconds: 3600   # 1 hours
image:
  repository: "alpine/kubectl"
  tag: "1.34.1"
  pullPolicy: IfNotPresent
rbac:
  serviceAccountName: cleanup-sa
