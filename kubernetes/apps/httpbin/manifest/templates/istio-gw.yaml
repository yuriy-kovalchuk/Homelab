apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: httpbin-istio-gw
  namespace: httpbin
spec:
  gatewayClassName: istio
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "httpbin.yuriy-lab.cloud"
