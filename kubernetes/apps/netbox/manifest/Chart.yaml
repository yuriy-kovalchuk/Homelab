apiVersion: v2
name: netbox-yk
description: Umbrella Helm chart for netbox application in Kubernetes

type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: "netbox"
    version: "7.2.15"
    repository: "https://charts.netbox.oss.netboxlabs.com"
