apiVersion: v2
name: external-secrets-yk
description: Umbrella Helm chart for external-secrets application in Kubernetes

type: application
version: 1.1.0
appVersion: "1.1.0"

dependencies:
  - name: "external-secrets"
    version: "1.2.0"
    repository: "https://charts.external-secrets.io"
