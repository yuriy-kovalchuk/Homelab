apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboards-cm
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
{{- $files := .Files }}
{{- range $path, $_ :=  $files.Glob "dashboards/*.json" }}
  {{ base $path }}: |-
{{ $files.Get $path | indent 4 }}
{{- end }}