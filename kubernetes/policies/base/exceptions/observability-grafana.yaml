apiVersion: policies.kyverno.io/v1beta1
kind: PolicyException
metadata:
  name: grafana
  namespace: grafana
spec:
  matchConditions:
    - name: exclude-grafana
      expression: "object.metadata.namespace == 'grafana'"
  policyRefs:
    - kind: ValidatingPolicy
      name: restrict-seccomp-strict
