apiVersion: v1
kind: Service
metadata:
  name: llama-cpp-rocm
  namespace: llm
spec:
  selector:
    app: llama-cpp-rocm
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
