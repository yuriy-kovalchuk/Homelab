apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: llama-cpp
  namespace: llm
spec:
  parentRefs:
    - name: internal
      namespace: main-gateway
      sectionName: https-llama-cpp
  hostnames:
    - "llama.yuriy-lab.cloud"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: llama-cpp
          port: 8080
