apiVersion: v1
kind: Service
metadata:
  name: llama-cpp-cuda
  namespace: llm
spec:
  selector:
    app: llama-cpp-cuda
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
