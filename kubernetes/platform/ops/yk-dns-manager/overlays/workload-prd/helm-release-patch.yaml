apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: yk-dns-manager
  namespace: yk-dns-manager
spec:
  suspend: true
  values:
    domainMap:
      "*.yuriykovalchuk.dev": "10.0.4.50"

      "s3.yuriykovalchuk.dev": "10.0.3.3"
      "rustfs.yuriykovalchuk.dev": "10.0.3.3"

