apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: yk-dns-manager
  namespace: yk-dns-manager
spec:
  suspend: true
  values:
    domainMap:
      "*.yuriykovalchuk.dev": "10.0.4.50"
      "truenas.yuriykovalchuk.dev": "10.0.4.60"

      "s3.yuriykovalchuk.dev": "10.0.4.60"
      "api.s3.yuriykovalchuk.dev": "10.0.4.60"

