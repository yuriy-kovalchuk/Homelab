apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: flux
  namespace: flux-system
spec:
  parentRefs:
    - name: internal
      namespace: kube-system
      sectionName: https-fluxcd
  hostnames:
    - "fluxcd.yuriykovalchuk.dev"
  rules:
    - backendRefs:
        - name: flux-operator
          port: 9080
