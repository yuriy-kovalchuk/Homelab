apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: forgejo
  namespace: forgejo
spec:
  values:
    httpRoute:
      enabled: true
      parentRefs:
        - name: internal
          namespace: kube-system
          sectionName: https-forgejo
      hostnames:
        - forgejo.yuriykovalchuk.dev

    tcpRoute:
      enabled: true
      parentRefs:
        - name: internal
          namespace: kube-system
          sectionName: ssh-forgejo
      port: 22
