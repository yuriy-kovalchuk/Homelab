apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: goldilocks
  namespace: vpa
spec:
  values:
    controller:
      podSecurityContext:
        seccompProfile:
          type: RuntimeDefault
      securityContext:
        seccompProfile:
          type: RuntimeDefault
    dashboard:
      podSecurityContext:
        seccompProfile:
          type: RuntimeDefault
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      httpRoute:
        enabled: true
        parentRefs:
          - name: internal
            namespace: kube-system
            sectionName: https-goldilocks
        hostnames:
          - goldilocks.yuriykovalchuk.dev
