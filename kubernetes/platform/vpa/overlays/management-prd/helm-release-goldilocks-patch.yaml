apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: goldilocks
  namespace: vpa
spec:
  values:
    dashboard:
      httpRoute:
        enabled: true
        parentRefs:
          - name: internal
            namespace: kube-system
            sectionName: https-goldilocks
        hostnames:
          - goldilocks.mgmt.yuriykovalchuk.dev
