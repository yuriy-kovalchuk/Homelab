apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: flux
  namespace: flux-system
spec:
  parentRefs:
    - name: internal
      namespace: kube-system
      sectionName: https-flux
  hostnames:
    - "flux.yuriy-lab.cloud"
  rules:
    - backendRefs:
        - name: flux-operator
          port: 9080
