apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
# Add workload-prd-specific patches and resources here when the workload
# cluster is provisioned (see REFACTOR.md).
