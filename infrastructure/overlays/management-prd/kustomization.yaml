apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
# Add management-prd-specific patches and resources here as components are
# migrated from management-cluster/fluxcd/infrastructure/ (see REFACTOR.md).
