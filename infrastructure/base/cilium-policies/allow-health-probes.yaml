apiVersion: cilium.io/v2
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: allow-health-probes
spec:
  description: "Allow kubelet liveness/readiness probes and Cilium node health checks"
  endpointSelector: {}
  ingress:
    - fromEntities:
        - host
        - remote-node
        - health
