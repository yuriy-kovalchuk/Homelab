apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: open-webui
  namespace: open-webui
spec:
  values:
    persistence:
      enabled: true
      storageClass: longhorn
      size: 5Gi
