customization:
  systemExtensions:
    officialExtensions:
%{ for ext in extensions ~}
      - ${ext}
%{ endfor ~}
%{ if length(extra_kernel_args) > 0 ~}
  extraKernelArgs:
%{ for arg in extra_kernel_args ~}
    - ${arg}
%{ endfor ~}
%{ endif ~}
